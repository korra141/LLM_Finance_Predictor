# LLM Finance Predictor + Reasoner

**Goal:** Finetune a base LLM to (a) **forecast short-term stock movement/returns** given past time series + textual context, and (b) **generate rationale / explanation** for its prediction.

## 🚀 Setup & Installation

```bash
git clone git@github.com:korra141/llm-finance-predictor.git
cd llm-finance-predictor
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
